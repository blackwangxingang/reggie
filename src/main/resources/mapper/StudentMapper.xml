<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.itheima.reggie.mapper.StudentMapper">


    <select id="query" resultType="com.itheima.reggie.dto.StudentDto">
        select student.*, classroom.class_name as class_name
        from student
        left join classroom on student.class_id = classroom.id
    </select>
</mapper>